import { ConvertDateTimePipe } from './convert-date-time.pipe';

describe('ConvertDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
