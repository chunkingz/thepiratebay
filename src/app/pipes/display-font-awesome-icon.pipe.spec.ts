import { DisplayFontAwesomeIconPipe } from './display-font-awesome-icon.pipe';

describe('DisplayFontAwesomeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayFontAwesomeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
